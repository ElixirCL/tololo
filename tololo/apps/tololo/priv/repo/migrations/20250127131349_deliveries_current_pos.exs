defmodule Tololo.Repo.Migrations.DeliveriesCurrentPos do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:deliveries) do
      modify :state, :text, default: "Init"
      add :current_latitude, :float
      add :current_longitude, :float
    end
  end

  def down do
    alter table(:deliveries) do
      remove :current_longitude
      remove :current_latitude
      modify :state, :text, default: nil
    end
  end
end
