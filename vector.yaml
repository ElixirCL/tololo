sources:
  in:
    type: "stdin"

sinks:
  out:
    inputs:
      - "in"
    type: "console"
    encoding:
      codec: "text"
  loki:
    type: loki
    encoding:
      codec: "text"
    inputs:
      - "in"
    labels:
      source: "eatsbot"
    endpoint: http://localhost:3100
